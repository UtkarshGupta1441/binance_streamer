# =============================================================================
# Binance Streamer - Pinned Dependencies
# Python 3.12 compatible | Last verified: 2026-06-14
# =============================================================================

# --- Core UI & Data ---
streamlit==1.50.0
pandas==2.3.3
plotly==6.3.1
numpy==1.26.4

# --- Streamlit's dataframe backend (must match streamlit version) ---
pyarrow==24.0.0

# --- Networking ---
requests==2.32.5
websocket-client==1.9.0

# --- Rust build tooling (for binance_streamer native extension) ---
maturin==1.14.0
.